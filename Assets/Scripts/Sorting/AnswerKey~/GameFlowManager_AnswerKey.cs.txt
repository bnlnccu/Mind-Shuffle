===== Answer Key for GameFlowManager.cs =====

===== Fill-in-the-blank A (Spawn) =====

        int randomIndex = Random.Range(0, stimulusPrefabs.Count);
        currentStimulus = Instantiate(stimulusPrefabs[randomIndex], spawnPoint.position, Quaternion.identity);

===== Fill-in-the-blank A (Destroy) =====

        Destroy(currentStimulus);

===== Fill-in-the-blank D (CSV Log) =====

        Debug.Log("@DATA," + trialCounter.CurrentTrial + "," + ruleBroadcaster.CurrentRule + "," + ruleBroadcaster.DidSwitchThisTrial + "," + currentStimulusName + "," + rt.ToString("F1") + "," + lastTrialCorrect);
